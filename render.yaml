services:
  - type: web
    name: case-demo
    runtime: docker
    dockerfilePath: Dockerfile
    healthCheckPath: /health
    envVars:
      - key: CASE_DB_PATH
        value: /data/case_audit.db
    disk:
      mountPath: /data
      sizeGB: 1
    plan: free
    autoDeploy: true
